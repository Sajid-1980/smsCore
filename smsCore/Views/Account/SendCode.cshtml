@model  SendCodeViewModel
@{
    ViewBag.Title = "Send";
}

<h2>@ViewBag.Title.</h2>

@*@using (Html.BeginForm("SendCode", "Account", new {Model.ReturnUrl}, FormMethod.Post, new {@class = "form-horizontal", role = "form"}))
{
  *@

<form asp-action="SendCode" asp-controller="Account"
                          data-ajax="true" data-ajax-method="post"  class = "form-horizontal form-label-left input_mask"  asp-route-returnUrl="@Model.ReturnUrl"   role="form">
                   
  
  
  @Html.AntiForgeryToken()
    @Html.Hidden("rememberMe", Model.RememberMe)
    <h4>Send verification code</h4>
    <hr/>
    <div class="row">
    <div class="col-md-8">
        Select Two-Factor Authentication Provider:
        <button type="submit" class="btn btn-default">Submit</button>
    </div>
</div>
</form>

@section Scripts {
    
    
    <script src="~/bundles.js" asp-append-version="true"></script>

}

}
@model FeeSlipPaymentMethod

@{
    ViewBag.Title = "Delete";
}


<h5>Are you sure you want to delete this?</h5><hr/>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PaymentMethodName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.PaymentMethodName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AllowPartialPayment)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AllowPartialPayment)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Campus.CampusName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Campus.CampusName)
        </dd>
    </dl>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <p>
        <button type="submit" class="btn btn-danger float-right"><i class="fa fa-trash mr-1"></i>Delete</button>
        <a href="@Url.Action("feepaymentmethod")" class="btn btn-dark"><i class="fa fa-chevron-circle-left"></i> Back</a>
    </p>
}
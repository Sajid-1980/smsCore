@model Microsoft.AspNetCore.Identity.IdentityRole

@inject StaticResources resources

@{
    ViewData["Title"] = "Delete";

    Layout = null;

}

<h5>Are you sure you want to delete this?</h5>
<div>
    <hr/>
    <dl class="dl-horizontal">
        <dt>Role</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
@*@using (Ajax.BeginForm("DeleteRole", "Users", new AjaxOptions {HttpMethod = "POST", OnSuccess = "hidedeletebtn", OnComplete = "completed", LoadingElementId = "classFeeIndexmainForm #server-progress", OnBegin = "begin"}, new {id = "classFeeIndexmainForm"}))
{*@
<form asp-action="DeleteRole" asp-controller="Users" data-ajax="true" data-ajax-method="post" data-ajax-complete="completed" data-ajax-begin="begin" class="form-horizontal form-label-left input_mask" id="classFeeIndexmainForm" data_reset="true">
  
    <div class="row">
        <div class="col-12">
            <div id="message">

            </div>
        </div>
    </div>
    @Html.HiddenFor(model => model.Id)
    <button asp="Index" data-dismiss="modal" aria-label="Close" class="btn btn-danger float-right ml-1"> <i class="fa fa-chevron-circle-left mr-2"></i> Cancel and Close</button>
    <button type="submit" value="Delete" id="deletebtn" class="btn btn-primary float-right">
        <i class="fa fa-save mr-2"></i>
        <span>
            <i id="server-progress" style="display: none;" class=" fa fa-spinner fa-spin "></i>
        </span> Delete
    </button>
<</form>
<script @resources.JavaScript>
    function hidedeletebtn() {
        $('#deletebtn').hide();
    }
</script>
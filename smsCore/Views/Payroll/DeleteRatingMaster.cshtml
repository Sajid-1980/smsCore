@model  RatingMaster

@inject StaticResources resources

@{
    ViewBag.Title = "DeleteRatingMaster";
    Layout = null;
}

<h5>Are you Sure Yo want to delete !!! </h5>

<div class="col-12">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(m => m.ID)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.ID)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.Description)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.StartDate)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.StartDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.CloseDate)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.CloseDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.Year)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.Year)
        </dd>
    </dl>

</div>

@*@using (Ajax.BeginForm("DeleteRatingMaster", "Payroll", new AjaxOptions {HttpMethod = "POST", OnComplete = "completed", LoadingElementId = "classFeeIndexmainForm #server-progress", OnBegin = "begin"}, new {id = "classFeeIndexmainForm"}))
{*@
    
<form asp-action="DeleteRatingMaster" asp-controller="Payroll"  data-ajax="true" data-ajax-method="post" data-ajax-complete="completed" data-ajax-begin="begin" class="form-horizontal form-label-left input_mask" id="classFeeIndexmainForm" data_reset="true">
  
                   
    @Html.HiddenFor(m => m.ID, new {@class = "form-control has-feedback-left"})
    <div class="row">
        <div class="col-12">
            <div id="message">

            </div>
        </div>
    </div>
    <div class="col-12">
        <div class="row">

            <div class="form-group col-12 pb-2 mt-3">
                <a href="@Url.Action("RatingMasterEntry", "Payroll")" aria-label="Back to List" class="btn btn-primary float-right"> <i class="fa fa-chevron-circle-left mr-2"></i> Back </a>
                <button type="submit" value="Create" class="btn btn-danger float-right">
                    <i class="fa fa-trash mr-2"></i>
                    <span>
                        <i id="server-progress" style="display: none;" class=" fa fa-spinner fa-spin "></i>
                    </span> Delete
                </button>
            </div>
        </div>
    </div>
</form>
@using sms.Models
@{
    ViewBag.Title = "ActiveRating";
    //if (Request.IsAjaxRequest())
    //{
    //    Layout = null;
    //}
}

<div class="breadcrumbs ace-save-state" id="breadcrumb">
    <ul class="breadcrumb">
        <li>
            <i class="fa fa-home ace-icon home-icon">

            </i>
            <a href="@Url.Action("Index", "Dashboard")">Dashboard</a>
        </li>
        <li class="active">
            Active Rating
        </li>

    </ul>
</div>
<section class="content">
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <div class="card-header with-border">
                    <h3 class="card-title">Select Class</h3>
                </div><!-- /.box-header -->
              @*  @using (Ajax.BeginForm("ActiveRating", "Payroll", new AjaxOptions {HttpMethod = "POST", OnComplete = "completed", LoadingElementId = "classFeeIndexmainForm #server-progress", OnBegin = "begin"}, new {id = "classFeeIndexmainForm"}))
                {*@
                <form asp-action="ActiveRating" asp-controller="Payroll" data-ajax="true" data-ajax-method="post"   data-ajax-complete="completed" data-ajax-begin="begin" class="form-horizontal form-label-left input_mask" id="classFeeIndexmainForm" data_reset="true">
                   
                    <div class="card-body">
                        <div class="row">
                            <div class="col-12">
                                <div id="message">

                                </div>
                            </div>
                        </div>


                        <div class="row" id=rating>
                            <div class="col-12 form-group has-feedback">
                                <label>Rating For</label>
                                <div class="controls">
                                    @Html.DropDownList("RatingId", (SelectList)ViewBag.Ratinglist, new { @class = "form-control" })
                                    <span class="fa fa-file-text form-control-feedback left" aria-hidden="true"></span>
                                    @Html.ValidationMessage("RatingId")
                                </div>
                            </div>
                            @*<div class="col-12 col-md-6 form-group has-feedback">
                                <label>Compus</label>
                                @Html.DropDownList("compusid", (SelectList)ViewBag.compusList, new { @class = "form-control" })
                                <span class="fa fa-university form-control-feedback left" aria-hidden="true"></span>
                                @Html.ValidationMessage("compusid")
                            </div>
                            <div class="col-12 col-md-6 form-group has-feedback">
                                <label>Class</label>
                                @Html.DropDownList("classid", (SelectList)ViewBag.ClassList, new { @class = "form-control" })
                                <span class="fa fa-list-alt form-control-feedback left" aria-hidden="true"></span>
                                @Html.ValidationMessage("classid")
                            </div>*@
                            @Html.Partial("_PartialCampusSelector", new PartialCampusViewModels {ParentContainerID = "rating", ColCssClass = "col-12 col-md-6 form-group has-feedback", ClassesFieldName = "classid", CampusFieldName = "compusid", ShowCampus = true, ShowClasses = true})

                            <div class="col-12 form-group has-feedback">
                                <label>Name</label>
                                <div class="controls">
                                    @Html.DropDownList("staffId", (SelectList)ViewBag.staff, new { @class = "form-control" })
                                                                <span class="fa fa-user form-control-feedback left" aria-hidden="true"></span>
                                                                @Html.ValidationMessage("staffId")
                                                            </div>
                            </div>


                        </div>
                    </div>
                    <div class="card-footer">
                        <div class="form-group col-12 pb-2 mt-3">
                            @*<a href="@Url.Action("Index", "Dashboard")" aria-label="Back to Dashboard" class="btn btn-danger"> <i class="fa fa-chevron-circle-left mr-2"></i> Back to Dashboard</a>
                            <button type="reset" value="New" class="btn btn-danger "><i class="fa fa-refresh mr-1"></i>Reset</button>*@
                            <button type="submit" value="Create" class="btn btn-primary float-right ">
                                <i class="fa fa-save mr-2"></i>
                                <span>
                                    <i id="server-progress" style="display: none;" class=" fa fa-spinner fa-spin "></i>
                                </span> Save
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-md-8">
            <div class="card card-primary">
                <div class="card-header ptbnull">
                    <h3 class="card-title titlefix">Details fo Rating</h3>
                </div><!-- /.box-header -->
                <div class="card-body">
                    <div class="row mt-3">
                        <div id="timetableman-gridContainer" class="col-12">
                            <table id="jqgTimetable" cellpadding="0" cellspacing="0"></table>
                            <div id="jqgPTimetable" style="text-align: center;"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@model  FeeSlipReceipt
@{
    ViewBag.Title = "Fee Slip Receipt Delete";
    //if (Request.IsAjaxRequest())
    //{
    //    Layout = null;
    //}
}

@*@using (Ajax.BeginForm("FeeSlipReceiptDelete", "Fee", new AjaxOptions { HttpMethod = "POST", OnSuccess = "reloadGridData", OnComplete = "completed", LoadingElementId = "FeeSlipReceiptDeletemainForm #server-progress", OnBegin = "begin" }, new { id = "FeeSlipReceiptDeletemainForm" }))
{*@
<form asp-action="FeeSlipReceiptDelete" asp-controller="Fee"
      data-ajax="true" data-ajax-method="post" enctype="multipart/form-data" data-ajax-complete="completed" data-ajax-begin="begin" class="form-horizontal form-label-left input_mask" id="FeeSlipReceiptDeletemainForm" data_reset="true">
                   



    @Html.AntiForgeryToken()

    <div class="row">
        <div class="col-12">
            <div>
                <h5>Are You Sure You Want to delte it.</h5>
            </div>
            <div id="message">

            </div>
        </div>
    </div>
    <div class="form-group col-12 mt-2 pb-2">
        <a href="@Url.Action("EditFeeIndex", "Fee")" aria-label="Back to Dashboard" class="btn btn-danger"> <i class="fa fa-chevron-circle-left mr-2"></i> Back to Fee Slip</a>
        <button type="submit" value="Create" class="btn btn-danger pull-right">
            <i class="fa fa-trash mr-2"></i> <span>
                <i id="server-progress" style="display:none;" class=" fa fa-spinner fa-spin "></i>
            </span> Delete
        </button>
    </div>
</form>

